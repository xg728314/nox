node_modules/
.next/
.env.local
.env
.env.*.local
orchestration/config/local.secrets.json
orchestration/scripts/.env

# SECURITY (SECURITY_FIX_PLAN.md TASK 0-1): never commit real MCP secrets.
# `.mcp.json.example` is the committed template; real values live only in `.mcp.json`.
.mcp.json

# Latency probe / diagnostic scratch dirs
.next_old_*/

# Monitoring bot state (per-host, never committed)
monitoring/state/

# Cleanup audit JSON output (regenerated by `npm run cleanup:audit`)
CLEANUP_AUDIT_FULL.json

# next build artifacts
.next/
.next_old_*/
out/
.vercel/

.vercel
.env*.local

# Playwright E2E (R23)
test-results/
playwright-report/
playwright/.cache/
.playwright/

.vercel
.env*.local

service.yaml
service.*.yaml

service.yaml
service.*.yaml

# ─────────────────────────────────────────────────────────────────────
# Capacitor / Android — generated build artifacts (commit X)
# ─────────────────────────────────────────────────────────────────────
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/release/
android/app/debug/
android/captures/
android/.idea/
android/.cxx/
# Capacitor sync 결과물 (sync 시 자동 재생성)
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json

# iOS Capacitor (Mac 환경에서만)
ios/App/build/
ios/App/Pods/
ios/.idea/
ios/App/output/
ios/DerivedData/

# Capacitor 작업 디렉토리
.capacitor/

# Claude Code session locks
.claude/scheduled_tasks.lock
